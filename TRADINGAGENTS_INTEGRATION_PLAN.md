# TradingAgents 与 Qlib 融合实施计划

日期：2026-09-07。状态：第一版离线融合基础已实施；后续阶段见第 8 节。

## 1. 目标与分工

建设一个能把定性研究、量化信号、组合约束、回测和复盘连起来的研究平台。核心研究问题是：加入 TradingAgents 的研究信息后，相比相同股票池和执行规则下的简单策略或 Qlib 模型，是否产生可验证的增益？

| 模块 | 职责 | 交付给下游的内容 |
| --- | --- | --- |
| TradingAgents | 新闻、基本面、情绪分析、多空论证 | 带证据、时间、有效期和版本的研究观点 |
| Qlib | 特征、滚动训练、评分、实验管理 | 带目标周期与基准的量化候选及评分 |
| quant_research | 组合、现金、风险、执行模拟、绩效 | 目标仓位、模拟成交、账本、风险及归因 |
| Dashboard | 候选、研究报告、组合、实验展示 | 可追溯到输入和交易的研究工作台 |

保留两个仓库及独立 Python 环境。第一版采用 JSON/JSONL 文件协议与独立进程调用；用户操作入口统一到 Qlib Dashboard，TradingAgents 原有 CLI/Web 保持可独立使用。已有 FastAPI 任务接口可以在后续适配阶段接入，无需先引入额外消息队列或微服务集群。

## 2. 当前事实与前置修正

本计划基于本地代码检查，不把此前的测试通过或“完成”描述当作完整验收。原研究设计仍有未交付部分，融合不能把它们视为已实现。

已具备：TradingAgents 的 `TradingAgentsGraph.propagate()`、五档评级、报告保存和 Web 任务接口；Qlib 的 Nasdaq 研究脚本，以及 `apps/quant_research` 中的账户、选股、联合因子和 ETF/Put 情景比较原型。

需要处理的具体问题：

1. `backtest.py::_protective_put` 的 fallback 波动率读取了起点之后的收益，早期存在未来信息；滚动卖出使用上次循环估值，需要按当前时点重新估值后成交。
2. `account.py::trade_equity` 检查总现金而不是可用现金；股票买入可能占用卖 Put 的预留资金。买入其他期权也应统一检查所有预留资金。
3. 波动率目标策略满仓时未为买入手续费留钱，可能出现负现金；回测还没有统一使用 Account 账本。
4. 行情入口只取原始 Close，尚未处理分红现金流；绩效以首次交易后净值为分母，遗漏初始费用影响。需统一初始资金、执行价格、现金收益、分红拆股和绩效口径。
5. TradingAgents 的历史记忆读取没有分析时点参数；执行前会解析待复盘记录并获取后续收益。历史回放必须隔离记忆，收益只有在回放时点已经发生且可用后才能反馈。
6. TradingAgents 的结构化模型对象最终会转为报告文本，融合接口不能假设已存在完整、稳定的结构化信号导出。

上述问题先补可失败的回归测试，再修复并重跑基线。已有 2020–2024 报告保留为旧版本开发结果，不能直接作为融合效果的对照结论。

## 3. 第一版使用流程

1. 用户选研究股票池、基准、调仓周期和模拟账户。默认 Nasdaq 候选以 QQQ 为基准，每周决策，5 个交易日评价；SPY 独立成组。
2. Qlib 或简单动量规则生成 Top-N 候选；TradingAgents 对少量候选生成研究报告。股票池可先是用户固定观察池，但历史结果必须标明固定池选择偏差。
3. 适配器检查数据时效、研究周期与字段，将研究信息关联到候选。默认先只展示；通过显式配置才能参与仓位决策。
4. 固定组合规则计算目标仓位，账户模块检查现金、集中度、换手和风险，生成模拟订单。
5. 回放净值、成交、未成交原因和相对基准结果；用户能从某笔交易追溯到候选评分、研究报告及规则版本。

第一版采用“研究观点作为候选过滤/降低仓位条件”，规则在实验前冻结。五档评级属于有序类别，不能直接解释成收益率或概率。Sell 在多头组合中表示减仓或排除，不自动建立空头。缺失报告和数据故障使用独立状态，不自动转为 Hold；新建仓位按冻结规则跳过，现有仓位按基础策略及风险规则处理。

## 4. 接口与时间约束

制定 `research_signal.v1`，由两个项目共同使用协议规范和测试样本，不直接依赖对方内部 Python 类。

| 字段组 | 必须记录 |
| --- | --- |
| 身份 | schema_version、signal_id、run_id、instrument_id、symbol、currency、benchmark |
| 时间 | analysis_as_of、generated_at、available_at、valid_until、horizon_trading_days |
| 观点 | rating、status、摘要、risk_flags、evidence_refs；价格目标可选 |
| 来源 | 数据快照 ID/哈希、来源与发布时间、模型/提示词/配置版本、两仓库代码版本及工作区差异指纹 |
| 实验 | research_mode、experiment_id、输入候选集 ID、失败原因、调用次数、token 与费用记录 |

由程序记录实际生成和入库时间，不能由 LLM 自报或依据 analysis_as_of 回填。实时信号必须在生成完成并入库后才可使用。历史重建单独记录模拟可用时间，并标记为 retrospective；今日生成的历史日期报告不能伪装成当时已产生的信号。

所有时间存 UTC，决策和执行按交易所日历处理。信号过期、周期不一致、证据缺失、未知协议版本、重复 ID 或时间不合法时给出明确处理结果。研究在收盘后完成时，最早只能参与之后可执行的交易时点；未赶上下一开盘则顺延或过期。

严格历史回放必须使用当时可得的数据快照、财报发布日期、宏观历史版本和历史成员资格。仅过滤网页或行情日期不能证明 LLM 无未来知识：模型训练数据可能包含后来的事件。因此历史 LLM 重建用于探索，前瞻逐日归档与模拟运行才用于积累更可信的新增证据。

## 5. 阶段、交付与验收

| 阶段 | 主要工作 | 验收条件 |
| --- | --- | --- |
| P0 基础审计与修正 | 修复第 2 节问题，统一 Account 账本及成本；建立数据/代码清单 | 未来价格扰动不改变历史决策与净值；担保资金不重复占用；逐笔资产守恒；初始费用和分红拆股正确；重跑基线 |
| P1 信号协议与离线导入 | TradingAgents 结构化导出、Qlib 导入/校验、报告关联、异常状态 | 固定样本能离线贯通；重复导入幂等；错误字段、未来时间、过期与缺数据有测试；保存来源和版本 |
| P2 候选研究任务 | 候选批量导出、独立进程运行 TradingAgents、超时/重试/预算/缓存、运行状态 | 同一输入按配置复用归档；失败可恢复且不重复入库；不同任务的配置与记忆不串用；单次真实调用烟测单独记录费用 |
| P3 组合与回放 | 可配置基础策略及研究过滤策略；时间对齐、持仓缓冲、现金/行业/单股限制 | 固定归档不调用 LLM 即可复现目标仓位、成交、账本与指标；未成交和风险拒绝可追溯 |
| P4 对照实验 | 相同池、基准、周期、成本下比较简单规则、Qlib、研究评级、融合策略 | 输出总收益与超额收益、回撤、换手、风险、成本和跨窗口结果；不得把固定池和 PIT 池结果混比 |
| P5 统一界面与前瞻模拟 | 候选/报告/组合/对照实验页面，定期归档信号并模拟执行 | 页面展示数据与信号时间、模式和版本；前端构建及主要流程测试通过；一次全流程运行可重放 |
| P6 继承 Put 研究路线 | 同一账户加入减仓/Put 方案，接入报价、合约生命周期和风险归因 | 模型情景与真实报价结果分开；真实路径需报价覆盖验收及到期/指派测试；不给缺报价实验贴真实成交标签 |

顺序：P0 → P1 → P2/P3 → P4 → P5；P6 在账户及数据条件具备后进行。每阶段完成后更新过程记录，未满足验收时保留未完成状态。

P1 的独立成果是一份已归档报告可以导入 Qlib 并关联候选；P3 的独立成果是同一信号文件能回放账户；P5 完成第一版端到端融合。P6 延续原多资产设计，不能因股票融合完成而宣称期权设计全部完成。

## 6. 实验方案

固定同一研究池和候选形成规则，至少比较：ETF 基准、等权/简单动量、Qlib 单独、TradingAgents 评级规则、Qlib 加研究过滤。若 TradingAgents 只评估 Qlib Top-N，则注明评级组也受 Qlib 候选预筛选，另报告该候选池等权对照。

先验证研究信息是否改善风险过滤，再评估排序加权，避免第一轮同时调多个自由参数。模型的 3–6 月建议不能未经转换直接用于 5 日策略；周期冲突时只展示，并记录未参与决策原因。

使用滚动训练/验证与标签成熟时间检查；确定并冻结实验参数后设定评估区间。已有反复查看的年份作为开发/压力区间。报告不同年份和市场环境、交易成本敏感性、LLM 版本与调用重复性、报告缺失率及样本覆盖；不以回测盈利作为工程验收条件。

真实历史成分、退市行情、新闻/财报快照不完整时，输出明确的数据覆盖限制，继续推进离线协议和前瞻模拟。历史期权报价需要用户已有数据或后续确定来源与预算，不能由定价模型填充后当成真实报价。

## 7. 拟新增或调整的文件

| 项目 | 拟调整位置 |
| --- | --- |
| Qlib | `docs/integration/research_signal_v1.md`：协议与兼容规则 |
| Qlib | `apps/quant_research/integrations/tradingagents.py`：导入与时间校验 |
| Qlib | `apps/quant_research/strategies/research_overlay.py`：研究信号组合规则 |
| Qlib | `apps/quant_research/experiments/`：可复现对照实验与运行清单 |
| Qlib | `apps/quant_research/tests/`：资金、未来信息、协议、组合回放测试 |
| Qlib | `apps/quant-dashboard/`：候选报告及融合实验展示 |
| TradingAgents | `tradingagents/integrations/`：结构化导出、候选输入及隔离运行入口 |
| TradingAgents | `graph/trading_graph.py`、`agents/utils/memory.py`：历史运行模式与按时点可用的记忆/复盘 |
| TradingAgents | `tests/`：导出协议、故障恢复、时间和任务隔离 |

以上是拟定路径，实施时按实际组织调整，不表示当前已创建。实施分支在两个仓库分别建立；先记录已有工作区改动，只提交与融合相关的变更。

## 8. 过程记录与完成标准

实施时在 Qlib 新增 `INTEGRATION_WORKLOG.md`，逐阶段记录问题、改动、命令、测试结果、数据限制和待办。每次实验保存 manifest、冻结配置、输入快照引用及哈希、signals、orders、fills、account_events、daily_nav、metrics 和报告。数据体积较大时不必提交 Git，但必须保留可访问的版本化文件及校验清单。

完成第一版需要同时证明：候选生成 → 研究报告与信号归档 → 校验 → 确定性组合与模拟成交 → 绩效对照 → Dashboard 全流程运行；协议与账户测试通过；用同一输入在离线环境复现回放；数据覆盖和历史/前瞻模式明确可见。

第一版已完成 P0 的关键修正、P1 的离线协议、P3 的确定性回放和冻结 fixture 的机制回测。具体命令、结果和仍未完成的 P2/P4/P5/P6 见 `INTEGRATION_WORKLOG.md`。真实 LLM 历史效果和期权报价回测仍未实施。
